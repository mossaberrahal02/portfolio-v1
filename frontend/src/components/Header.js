import React from 'react';

function Header() {
    return (
        <header style={{ padding: '20px', background: '#007bff'}}>
            <h1 style={{ color: 'white' }}>My Portfolio</h1>
        </header>
    );
}

export default Header;
